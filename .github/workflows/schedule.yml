name: schedule work flow
on:
  schedule:
    - cron: '0/5 * * * *'
  workflow_dispatch:
  workflow_run: 
    workflows: [feature-a-bdba]
    types:
      - completed
env:
  PROJECT_NAME: 1
jobs:
  task:
    runs-on: [ ubuntu-22.04 ]
    steps:
      - name: Setup 1
        run: |
          FULL_REPOSITORY=${{github.repository}}
          echo "仓库全称是${FULL_REPOSITORY}"
          
          repository=$(echo ${FULL_REPOSITORY} | cut -d '/' -f 1)
          echo 2222
          echo ${repository}
          echo 3333
        
