server:
  port: 8092

spring:
  application:
    name: cloud-service-dao

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8001/eureka

  instance:
    instance-id: springCloud-provider-dao-8082
    prefer-ip-address: true